package apex.functions;

import static org.assertj.core.api.Assertions.assertThat;

import org.openqa.selenium.WebDriver;

import apex.pages.CheckoutPage;
import apex.pages.CheckoutPage.CheckoutActions;
import apex.pages.MainPage;
import apex.pages.MainPage.MainActions;


public class OrderManagement {
    private MainPage mainPage;
    private MainActions mainActions;
    private CheckoutPage checkoutPage;
    private CheckoutActions checkoutActions;

    public OrderManagement(WebDriver driver) {
        this.mainPage = new MainPage(driver);
        this.checkoutPage = new CheckoutPage(driver);
        
        this.mainActions = mainPage.new MainActions();
        this.checkoutActions = checkoutPage.new CheckoutActions();
    }

    /**
     * adds dynamic item to cart
     */
    public void addPhoneToCart() {
    	System.out.println(mainPage.lblCloseCartQty.getAttribute("value"));
    	String currentCartQty = mainPage.lblCloseCartQty.getAttribute("text");
    	
    	Integer.parseInt(currentCartQty);
    	mainActions.clickDynamicAddButton(1);
    	assertThat(mainPage.lblCloseCartQty.getAttribute("text")).describedAs("Qty did not increase").isEqualTo(currentCartQty+1);
    	System.out.println(mainPage.lblCloseCartQty.getAttribute("text"));
    	
        
    }
    
    public void fillInDetails(String firstName, String lastName, String address, String province, String postalCode) {
    	checkoutActions.fillInCheckoutDetails(firstName, lastName, address, province, postalCode);
    }
}

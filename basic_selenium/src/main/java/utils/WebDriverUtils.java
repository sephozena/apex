package utils;

import org.openqa.selenium.WebDriver;

public class WebDriverUtils {
	protected WebDriver driver;
	
	public WebDriverUtils(WebDriver driver) {
		this.driver = driver;
	}
	
	/**
	 * set of custom webdriver utils
	 * pause
	 * wait
	 * elementPresent
	 */
}
